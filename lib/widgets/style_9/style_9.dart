import 'package:flutter/material.dart';

Widget recommendedSection_video() {
  return Container(
    margin: const EdgeInsets.symmetric(horizontal: 10),
    child: const Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        // Text with custom style

        // Row to hold the decorative line
/*
        Row(
          children: [
            // First container (black)
            Container(
              height: 5,
              width: 30,
              decoration: BoxDecoration(
                color: Color(0xffC8C8C8),
              ),
            ),
            SizedBox(width: 5),

            // Second container (red)
            Container(
              height: 5,
              width: 50, // Adjust the width dynamically based on title length
              decoration: BoxDecoration(
                color: Color(0xffC62326),
              ),
            ),

            // Spacer to push the black line to the center of the title
            Expanded(
              child: Container(
                height: 2.5,
                decoration: BoxDecoration(
                  color: Colors.black12,
                ),
              ),
            ),
          ],
        ),
*/
      ],
    ),
  );
}
